---
description: "Generate a terse Conventional Commits message for the current staged or described changes."
---

# Caveman Commit

Load the **caveman-commit** skill and generate a commit message for: **$ARGUMENTS**

If `$ARGUMENTS` is empty, inspect the staged changes and current diff summary. Do not create a commit unless the user explicitly asks.
